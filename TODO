
	Make option to change rules more in confortable, modular way
	Color custiomization:
	      Convert from RGBA to 0x0000
	Allow user to expand the world:
	        e.g. C-+ for add the strip of cells on every side
		and C-- for remove (dunno if "remove" is a proper
		word for that, there will be saving option the status of
		periferic cells 
	Writing commands in buffer for:
		generate specific struc	tures
		start/stop life + set time periods
	Further features:
		Swiping the map
		...?
		